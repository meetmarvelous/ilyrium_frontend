# Server Port
PORT=8080

# Redis Cache URL (used for rate-limiting and active API key validation)
REDIS_URL=localhost:6379

# Matrixed Link RPC Provider Template:
# https://eu.endpoints.matrixed.link/rpc/<network>?auth=<token>
# wss://eu.endpoints.matrixed.link/ws/<network>?auth=<token>

PRIMARY_RPC_URL=https://eu.endpoints.matrixed.link/rpc/solana-devnet?auth=YOUR_MATRIXED_AUTH_TOKEN
PRIMARY_WS_URL=wss://eu.endpoints.matrixed.link/ws/solana-devnet?auth=YOUR_MATRIXED_AUTH_TOKEN

# Fallback Solana RPC Provider (used automatically if primary fails/times out)
FALLBACK_RPC_URL=https://api.devnet.solana.com

# PostgreSQL / Supabase Database Connection String (for async usage log flushing)
DATABASE_URL=postgres://postgres:password@db.your-supabase-project.supabase.co:5432/postgres

# Background Usage Log Flush Interval in Seconds
FLUSH_INTERVAL_SECONDS=10
